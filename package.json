{
  "name": "hcp-mcp-proxy",
  "version": "1.0.0",
  "description": "MCP server for HouseCall Pro that embeds the HCP API key in the URL path. Bypasses Anthropic's broken OAuth flow.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
